# LMS Batch 4 — Instructor Dashboard UI

Copy the contents of this folder into your LMS project root.

Allow these files to overwrite:

```txt
assets/css/style.css
assets/js/app.js
```

New pages added:

```txt
pages/instructor/dashboard.html
pages/instructor/courses.html
pages/instructor/create-course.html
pages/instructor/course-builder.html
pages/instructor/students.html
pages/instructor/earnings.html
```

Recommended Git commands:

```powershell
git checkout main
git pull
git checkout -b feature/instructor-dashboard

git status
git add .
git commit -m "Add instructor dashboard UI"
git push -u origin feature/instructor-dashboard
```

Test pages in your browser after copying.
