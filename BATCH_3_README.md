# Batch 3 — Student Dashboard UI

Copy the contents of this folder into your project root.

Allow these files to overwrite your current versions:

```txt
assets/css/style.css
assets/js/app.js
```

They include the previous public/auth styles and new student dashboard styles/interactions.

## New Pages

```txt
pages/student/dashboard.html
pages/student/my-courses.html
pages/student/lesson-player.html
pages/student/assignments.html
pages/student/certificates.html
pages/student/profile.html
```

## Git Commands

```powershell
git checkout main
git pull
git checkout -b feature/student-dashboard

git status
git add .
git commit -m "Add student dashboard UI"
git push -u origin feature/student-dashboard
```
